import React from "react";
import "./Header.css";

const Header = props =>
<header className="header">
<h1>"To get the score, click on an image but don't click on any more than once!"</h1>
</header>

export default Header;
